# Backend Notes

Backend documentation is maintained in the repository root README:

- [Project README](C:/Users/tanis/Documents/dev/CrackedInfo/README.md)

Key runtime commands:

- API:
  - `uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload`
- Streamlit test UI:
  - `uv run streamlit run streamlit_app.py --server.address 0.0.0.0 --server.port 8501`
